on:
  pull_request:
    branches:
      - master
    types: [closed]
jobs:

  build:
    name: Build
    runs-on: self-hosted
    steps:
    - name: trigger single Job
      uses: appleboy/jenkins-action@master
      with:
        url: "https://aqjftr6vxxtfrjfrvcqqlu.hooks.webhookrelay.com"
        user: "admin"
        token: 11250bca5035cbbf7d4dac6d47998e664d
        job: "test webhook"
